# Compact engineering review scope

Start with [the closed-loop review](../../../DA_PLCBF_CLOSED_LOOP_SEARCH_REVIEW.md).
This directory extends the user-authorized compact publication to `main` over commit
`0bcd4a17b03d0fc99f4bdcc024b866090072fa43`.

The directory contains 71 distinct complete paired discovery episodes, a separate exact U132
reproduction check, deterministic and causal confirmations, actual paced runs, and independent
geometry/timing audits. It retains negative and failed completed outcomes. Unexecuted proposals,
offline generator checks, and partial attempts are identified and excluded from episode counts.

This is an engineering review bundle, not a sealed scientific campaign or an entry in `INDEX.md`.
It is incomplete for whole-directory replay without regenerating local bulk data. The numerical
claim concerns the modeled XML sphere and its recorded-state interpolation. A separately measured
MuJoCo contact continuation is visual evidence of physical contact response, not a second
closed-loop controller benchmark. No hardware or general safety guarantee is made.

The publication manifest binds every included and local-only file by path, size, and SHA-256.
All generated videos and large prediction/render tensors remain local. Eight representative
video stills are published; extra decoded inspection frames remain local with recorded hashes. Exact compact control
traces, dense flight states, saved scene definitions, checkpoints, source archives, and negative
results remain reviewable. Compact derivations record their source hashes and array equality.
Source archives at execution preserve the earlier implementation versions; the final source delta
records the final changes over the stated base commit.

The matched uncompensated-fallback case demonstrates continuous fixed contact versus adaptive
mission completion. The stronger compensated frozen comparator survives the selected case,
matched buffer sweep, and local neighborhood. The first paced fixed startup overrun and the late
parameter-reversion survival are retained alongside the successful final paced replay and early
causal intervention.
